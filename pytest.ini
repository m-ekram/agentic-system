[pytest]
# asyncio_mode = auto means async test functions just work, without having to
# decorate every one of them with @pytest.mark.asyncio.
asyncio_mode = auto
testpaths = .
python_files = test_*.py
addopts = -v
