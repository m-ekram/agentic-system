# Pinned to the versions the test suite passes against, so a deploy builds
# exactly what CI tested rather than whatever is newest that day.

# The agent loop. Works against any OpenAI-compatible endpoint
# (Gemini, Groq, Ollama) — see .env.example.
openai==3.6.0

# Typed tool arguments. Each tool's args are a Pydantic model, and
# model_json_schema() is what we hand to the LLM as the tool schema.
pydantic==2.13.5

# Official MCP SDK — exposes the same tools to Claude Desktop (stdio) and,
# when deployed, to Claude Code over HTTP.
mcp==2.1.1

# HTTP client for the fetch_url tool (our untrusted-input source).
httpx==0.28.1

# Loads API_KEY / BASE_URL / MODEL from .env
python-dotenv==1.2.3

# The eval harness. pytest-asyncio is needed because some tools are async.
pytest==9.1.1
pytest-asyncio==1.4.0

# The read-only audit log viewer; uvicorn also serves the MCP HTTP transport.
fastapi==0.141.1
uvicorn==0.52.4
